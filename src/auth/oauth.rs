// OAuth DPoP flow placeholder.
// jacquard's OAuthClient handles the full flow; this module
// wraps it for our use case.

// For now, OAuth is stubbed — we implement app password auth first
// since it's simpler to test locally, and add OAuth as a follow-up.
